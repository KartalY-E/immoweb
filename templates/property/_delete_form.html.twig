<form method="post" action="{{ path('property_delete', {'slug': property.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
